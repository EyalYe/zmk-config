# Shared TBK-NINI config — applied to left, right, and dongle builds.
# (ZMK loads this for every shield because they all reduce to the base
#  name "tbk_nini".) Keep only keyboard-wide settings here; role-specific
# options live in the per-shield confs under boards/shields/tbk_nini/.

CONFIG_ZMK_KEYBOARD_NAME="TBK-NINI"
